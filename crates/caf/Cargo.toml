[package]
name = "caf"
description = "Content addressable file store command-line tool"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
# This crate is not published independently.
publish = false

[dependencies]
anyhow = { workspace = true }
caf-format = { workspace = true }
caf-store = { workspace = true }
clap = { workspace = true }
# `dev corrupt-file --preset random`: seedable, reproducible corruption
# bytes. Random streams are reproducible within this implementation but
# are not compatible across implementations.
rand = { workspace = true }

[dev-dependencies]
assert_cmd = { workspace = true }
tempfile = { workspace = true }

[lints]
workspace = true
